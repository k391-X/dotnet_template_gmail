@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="vi">
<head>
    <meta charset="utf-8" />
    <title>Thông báo</title>
    <style>
        body {
            font-family: "Segoe UI", Arial, sans-serif;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: linear-gradient(135deg, #e3f2fd, #bbdefb);
            margin: 0;
        }

        .message-box {
            background: #fff;
            padding: 30px 25px;
            border-radius: 12px;
            text-align: center;
            box-shadow: 0 4px 15px rgba(0,0,0,0.15);
            width: 400px;
        }

        .message-box h2 {
            color: #0455A4;
            margin-bottom: 15px;
        }

        .message-box p {
            font-size: 16px;
            margin-bottom: 20px;
        }

        .message-box a {
            color: #0455A4;
            text-decoration: none;
            font-weight: bold;
        }

        .message-box a:hover {
            text-decoration: underline;
        }
    </style>
</head>
<body>
    <div class="message-box">
        <h2>Gửi email thành công!</h2>
        <p>Gmail đã được gửi đến email cá nhân của bạn. Vui lòng kiểm tra hộp thư và mục spam.</p>
        <p>Chuyển về trang đăng nhập sau <span id="countdown">20</span> giây...</p>
        <p><a href="/login">Click vào đây nếu không muốn chờ</a></p>
    </div>

    <script>
        let countdown = 20;
        const countdownElem = document.getElementById("countdown");

        const interval = setInterval(() => {
            countdown--;
            countdownElem.textContent = countdown;
            if (countdown <= 0) {
                clearInterval(interval);
                window.location.href = "/login";
            }
        }, 1000);
    </script>
</body>
</html>
